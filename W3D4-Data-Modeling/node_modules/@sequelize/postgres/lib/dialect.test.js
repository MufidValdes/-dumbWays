"use strict";
var import_core = require("@sequelize/core");
var import_postgres = require("@sequelize/postgres");
var import_chai = require("chai");
describe("PostgresDialect#parseConnectionUrl", () => {
  const dialect = new import_core.Sequelize({ dialect: import_postgres.PostgresDialect }).dialect;
  it("parses connection URL", () => {
    const options = dialect.parseConnectionUrl(
      "postgres://user:password@localhost:1234/dbname?client_encoding=utf8mb4"
    );
    (0, import_chai.expect)(options).to.deep.eq({
      host: "localhost",
      port: 1234,
      user: "user",
      password: "password",
      database: "dbname",
      client_encoding: "utf8mb4"
    });
  });
  it("accepts the postgresql:// scheme", () => {
    const options = dialect.parseConnectionUrl("postgresql://@localhost");
    (0, import_chai.expect)(options).to.deep.eq({
      host: "localhost"
    });
  });
});
//# sourceMappingURL=dialect.test.js.map
